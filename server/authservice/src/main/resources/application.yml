spring:
  application:
    name: authservice
  datasource:
    url: jdbc:mysql://localhost:3306/auth_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    database: mysql

server:
  port: 8055