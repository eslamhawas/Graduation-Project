spring:
  application:
    name: server
  datasource:
    url: jdbc:mysql://localhost:3306/nextronica
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database: mysql

server:
  port: 8055