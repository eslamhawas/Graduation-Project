spring:
  application:
    name: server
  datasource:
    url: jdbc:mysql://localhost:3306/auth_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    database: mysql

server:
  port: 8055